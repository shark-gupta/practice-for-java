package Arrays;
import java.util.Arrays;

public class maxAndMinElem {
    public static void main(String[] args) {
        int[] arr = {9,10,3,4,7,12,111,453,678};
        Arrays.sort(arr);
        System.out.println("max: " +arr[arr.length-1] + " min: " + arr[0]);
            }
        }

